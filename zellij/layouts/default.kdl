layout {
    default_tab_template {
        // Render the terminal panes first (top of screen)
        children
        
        // Render the bar afterwards (bottom of screen)
        pane size=1 borderless=true {
            plugin location="zellij:compact-bar"
        }
    }
}
