# --- VIM Navigation Bindings ---
bind-key j down
bind-key k up
bind-key h quit
bind-key l open
bind-key J next-feed
bind-key K prev-feed
bind-key g home
bind-key G end
bind-key d pagedown
bind-key u pageup

# --- Browser Plug (using w3m for inline reading) ---
browser "w3m %u"
macro , open-in-browser ;
bind-key o open-in-browser

# --- Omarchy Theme Integration (Dynamic Colors) ---
# We use standard ANSI colors (color0-15) so it matches your Omarchy theme automatically.
#
# --- Theme-Safe High Contrast ---
# This just swaps fg/bg for the selection, ensuring it is always readable.
color background         default   default
color listfocus          default   default   reverse bold
color listfocus_unread   default   default   reverse bold

# The Info bar at the top/bottom
color info               default   default   reverse

# Standard articles (unselected)
color listnormal         default   default
color listnormal_unread  blue      default   bold

# Article content
color article            default   default

# Text Content Highlights
highlight article "^(Title):.*$" blue default bold
highlight article "^(Author):.*$" cyan default
highlight article "^(Link):.*$" yellow default underline
highlight article "^(Date):.*$" green default

# --- Feeds List Format ---
feedlist-format "%4i %n %11u %t"
